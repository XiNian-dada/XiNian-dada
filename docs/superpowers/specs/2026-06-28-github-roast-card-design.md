# GitHub Roast Card Design

## Goal

Add the GitHub Roast card for `XiNian-dada` to the profile README without changing existing content or visual structure.

## Placement

Place the linked card in the existing `Stats` section, directly below the two current statistics images and before the section separator. Wrap it in a centered `<div>` so it aligns with the existing statistics block.

## Content

Use the supplied card image and profile link unchanged:

```markdown
[![GitHub Roast](https://githubroast.dev/api/card/XiNian-dada)](https://githubroast.dev/u/XiNian-dada)
```

## Verification

Confirm the Markdown contains the exact image and destination URLs, inspect the rendered structure for balanced HTML tags, and verify the diff contains only the intended README addition.
